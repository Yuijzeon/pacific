@using 第二組期末專題.Models
@model 用戶

<div class="tagcloud">
    @foreach (var hashtag in Model.GetHashtag清單())
    {
        <a href="/Search?hashtag=@hashtag["Id"]" class="tag-cloud-link">@hashtag["名稱"]</a>
    }
    @if (Model.Count == 0)
    {
        <p>此用戶沒有關注的 Hashtag !</p>
    }
</div>