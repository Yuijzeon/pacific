@if (Session["ID"] != null)
{
    <!--右下角按鈕-->
    <input type="button" href="/Post" class="btn1" title="查看旅程包" value="+">
    <input type="button" href="/Search?Favorite=@Session["ID"]" class="btn2" title="已收藏旅程" value="🧡">
    <!--右下角按鈕end-->
}